table! {
    coffee_beans (id) {
        id -> Integer,
        name -> Varchar,
        processing -> Varchar,
        roasting -> Varchar,
        origin -> Varchar,
        image_url -> Varchar,
    }
}
